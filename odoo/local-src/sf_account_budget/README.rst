Generic module to import the company cost budget in csv format.
- Each file contain the cost estimation (purchases) for the 12 monthly periods in a given fiscal year.
- The estimation is done per account.
- Several estimations are imported during one fiscal year (typically 12). Each new estimation improves the accuracy the previous one.
